// import { element } from 'aurelia-protractor-plugin/protractor';
// import {customAttribute, inject, bindingMode, TaskQueue} from 'aurelia-framework';
    
//     @customAttribute('focus2', bindingMode.twoWay)
//     @inject(Element)
//     export class Focus {
//       element: Element;
//       value: Boolean;
//       focusListener: any;
//       blurListener: any;
//       constructor(element) {
//         this.element = element;
      
//         this.focusListener = e => this.value = true;
//         this.blurListener = e => this.value = false;
//       }
      
      // attached() {
      //   this.element.addEventListener('focusss', this.focusListener);
      //   this.element.addEventListener('blur', this.blurListener);
      // }
      
      // detached() {
      //   this.element.removeEventListener('focusss', this.focusListener);
      //   this.element.removeEventListener('blur', this.blurListener);
      // }
    
    // } 
